# =============================================================================
# Test Environment Configuration
# =============================================================================
# Copy this file to .env.local for E2E testing without authentication
# This disables Supabase authentication so tests can run without sign-in
# =============================================================================

# Disable Supabase (allows tests to run without authentication)
VITE_SUPABASE_URL=""
VITE_SUPABASE_ANON_KEY=""

# Keep other settings as normal
VITE_PROXY_URL=http://localhost:3001
