# Railway Configuration for OneMind AI Backend

[build]
builder = "NIXPACKS"

[deploy]
# Disable healthcheck - server handles its own health
healthcheckPath = ""
healthcheckTimeout = 0
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
