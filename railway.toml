# Railway Configuration for Backend Deployment
# This tells Railway to deploy the Node.js backend from the server/ directory

[build]
builder = "NIXPACKS"

[deploy]
# Start command runs from the root, so we need to cd into server
startCommand = "cd server && node ai-proxy.cjs"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
